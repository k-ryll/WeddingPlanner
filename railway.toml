[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"
buildPath = "target"

[deploy]
startCommand = "java -jar target/WeddingPlanner-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure" 